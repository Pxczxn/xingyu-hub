# 星语小程序（xingyu-uniapp）

跨端小程序工程，使用 HBuilderX 或 uni-app CLI 开发。

## 开发

使用 HBuilderX 打开本目录，运行到微信开发者工具。

```bash
npm install
```

## 目录

- `pages/` — 页面
- `static/` — 静态资源
- `manifest.json` / `pages.json` — 应用配置
