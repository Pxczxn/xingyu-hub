"use client";
import Image from "next/image"; import Link from "next/link"; import { useRouter } from "next/navigation"; import { useEffect, useState } from "react"; import { CheckCircle2, Plus, Sparkles } from "lucide-react"; import { AppShell } from "@/components/layout/AppShell"; import { Alert } from "@/components/ui/alert"; import { Button } from "@/components/ui/button"; import { communityApi, type ArticleSummary } from "@/lib/community-api";
export default function DraftsPage(){const router=useRouter();const [items,setItems]=useState<ArticleSummary[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState<string|null>(null);const [creating,setCreating]=useState(false);useEffect(()=>{communityApi.listMyArticles().then(x=>setItems(x.filter(a=>a.status==="DRAFT"))).catch(()=>setError("无法加载草稿，请确认已登录")).finally(()=>setLoading(false))},[]);async function create(){setCreating(true);try{const d=await communityApi.createArticle();router.push(`/studio/articles/${d.articleId}/edit`)}catch{setError("创建草稿失败");setCreating(false)}}return <AppShell><main className="xy-drafts">{error&&<Alert variant="destructive">{error}</Alert>}<header><span><h1>草稿箱</h1><p>自动保存中，安心创作吧 <Sparkles/></p></span><Button onClick={()=>void create()} disabled={creating}><Plus/>{creating?"创建中…":"新建创作"}</Button></header><nav><b>全部草稿 <i>{items.length}</i></b><Link href="/studio/content?status=reviewing">审核中</Link><Link href="/studio/content?status=returned">已退回</Link><Link href="/studio/trash">回收站</Link></nav><section className="xy-drafts-grid"><div>{loading?<p>正在加载草稿…</p>:items.length?items.map((a,i)=><article key={a.id}><Image src={`/prototype-assets/drafts/draft-${(i%4)+1}.png`} alt="" aria-hidden="true" width={134} height={130}/><span><h2>{a.title||"未命名草稿"}</h2><small>{a.updatedAt?new Date(a.updatedAt).toLocaleString("zh-CN"):"更新时间暂未提供"}</small><p>{a.summary||"草稿摘要暂未提供"}</p></span><dl><div><dt>最后编辑</dt><dd>{a.updatedAt?new Date(a.updatedAt).toLocaleString("zh-CN"):"暂未提供"}</dd></div><div><dt>字数</dt><dd>暂未提供</dd></div><div><dt>自动保存</dt><dd><CheckCircle2/>状态待同步</dd></div></dl><Button variant="outline" asChild><Link href={`/studio/articles/${a.id}/edit`}>继续编辑</Link></Button></article>):<div className="py-16 text-center"><h2>这里还没有草稿</h2><p>开始创作后，草稿会自动保存。</p><Button onClick={()=>void create()}><Plus/>新建创作</Button></div>}</div><aside><div><h2>暂无草稿</h2><p>开始写下第一个故事吧</p><Button onClick={()=>void create()}><Plus/>新建创作</Button></div><section><h3><Sparkles/> 创作小贴士</h3><p>记录灵感，草稿箱会自动保存。</p></section></aside></section><footer><Sparkles/> 草稿保存状态以服务端数据为准</footer></main></AppShell>}
